# Dependabot configuration for wetechi-netmon
#
# Only ecosystems with a real manifest file in the repository are
# configured. As Phase 2 (Rust crates), Phase 6 (web app), and later
# container builds land, add:
#   - package-ecosystem: "cargo"     directory: "/"        (Phase 2+)
#   - package-ecosystem: "npm"       directory: "/apps/web" (Phase 6+)
#   - package-ecosystem: "docker"    directory: "/"        (once Dockerfiles exist)
# Each addition must have a corresponding entry validated in
# docs/dependency-license-matrix.md before being merged.

version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    labels:
      - "dependencies"
      - "github-actions"
    commit-message:
      prefix: "chore(deps)"
