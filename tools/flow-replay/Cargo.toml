[package]
name = "wetechinetmon-flow-replay"
description = "Sends synthetic IPFIX messages over UDP for safely testing the collector. Never sends real captured or attack traffic — see docs/security-principles.md."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "flow-replay"
path = "src/main.rs"

[dependencies]
tokio.workspace = true

[dev-dependencies]
wetechinetmon-protocol-ipfix = { path = "../../crates/protocol-ipfix" }
