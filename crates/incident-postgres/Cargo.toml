[package]
name = "wetechinetmon-incident-postgres"
description = "Milestone 5B-2: PostgreSQL schema and forward-only refinery migrations for the incident domain. No IncidentStore implementation, no connection pool wiring, no production database connection yet — see docs/development/follow-ups.md FU-42 and phase5-implementation-plan.md's 5B-3 section."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
wetechinetmon-incident = { path = "../incident" }
uuid.workspace = true
tokio-postgres.workspace = true
deadpool-postgres.workspace = true
rustls.workspace = true
tokio-postgres-rustls.workspace = true
refinery.workspace = true

[dev-dependencies]
# Already an approved, existing workspace dependency (vendored since
# Phase 2 in crates/collector, crates/storage, tools/flow-replay) — used
# here only to drive the async migration smoke test
# (tests/migration_smoke_test.rs), not a new dependency for this crate.
tokio = { workspace = true }
